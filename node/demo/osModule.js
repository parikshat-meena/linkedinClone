const os = require("os");

// os module is used for fetching information regarding operating system
console.log(os.arch());
console.log(os.homedir());
console.log(os.totalmem());
console.log(os.freemem());
console.log(os.platform());
// console.log(os.arch());
